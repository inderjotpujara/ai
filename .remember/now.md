
Confirmed — nothing new to save. The critical finding (phantom runs from missing RUN_ROOT_NAMES) was already documented in the existing constraint `[[ephemeral-run-root-span-contract]]` ("Routes calling withRunTelemetry must register root-span names in RUN_ROOT_NAMES or runs ghost as 'Running' forever"); this session just surfaced the specific violation instance (mcp.mount/recall/ingest). The other findings (SSE error-lane, .md file.type, docs endpoint naming) are bug instances being fixed, not durable patterns or preferences. Nothing to add to memory.
Confirm: No new durable facts. The constraint requiring root-span registration in `RUN_ROOT_NAMES` was already documented in [[ephemeral-run-root-span-contract]]; the Critical finding was a specific instance of that constraint being violated (three missing roots: `mcp.mount`, `memory.recall`, `memory.ingest`), now fixed. The SSE error-frame requirement is an implementation detail now in code. The resume pointer was updated by the agent. Nothing else warrants memory.